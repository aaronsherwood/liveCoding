drop_it = do
  -- this midi will select the video
  d1 $ ccv "<[1 ~ ~ 0 ~  ~ 4 ~] [~ 2 3 ~  <0 ~> <2 ~> ~ <0 ~>]>" # ccn "0" # s "midi"
  -- the rest is for sound
  d2 $ s "~ cp" # gain 1.2 # room 0.5
  d3 $ whenmod 4 3 (trunc 0.5) $ s "<feel:6(3,8) feel:6(5,8,1)>" # room 0.95 # speed (slow "< 4 1 1 1>" (range 8 2 saw)) # krush 5 # up "-2"

drop_it
